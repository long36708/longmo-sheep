// Pin the repository so generated CHANGELOG compare/commit links point at this
// fork (long36708/longmo-sheep) instead of the upstream Akryum/sheep.
// changelogen reads this file via `loadConfig({ name: 'changelog' })`.
export default {
  repo: 'long36708/longmo-sheep',
}
